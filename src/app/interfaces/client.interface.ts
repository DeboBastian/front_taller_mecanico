

export interface Client {
    id: number;
    name: string;
    surname: string;
    email: string;
    phone: string;
    dni: string;
    address: string;
    card_number: number; //(que se ponia para que no fuera obligatorio en el form)
 
}
